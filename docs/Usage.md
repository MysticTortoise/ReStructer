# Usage
Include "ReStructer.hpp" whenever you want to call ReadValue().

"ReStructerLast.hpp" should be included after most headers where possible, 
as some issues with template ordering make this necessary. 
It seems to work fine for now, but you never know later down the line. ;)